# 验证记录（2026-09-16）

## 1.3 检查更新

- 托盘加入手动检查；异步联网，重复点击被禁用，退出时取消请求。
- 11 项自检通过，新增正式版本筛选、独立工具 ID、数字版本比较、跨页候选保留、空列表处理验证。
- 使用程序内同一检查方法实际请求 GitHub 成功：本机 1.3.0，线上正式版 1.2.0，不会错误提示降级。
- 检查方法网络实测通过；托盘点击及打开浏览器交互尚待用户实测。不包含自动安装能力。

## 1.2 系统忙碌光标（当前版本）

- 按用户反馈移除 1.1 的浮动提示框，处理期间临时替换普通箭头为系统忙碌光标，结束后恢复。
- Release 构建 0 警告、0 错误。
- 系统光标验证 5/5 通过，报告：`artifacts/cursor-test.json`。
- 直接调用 Win32 绘制并比较系统光标像素，确认切换为原生忙碌光标，以及正常结束、重复开始、对象释放后恢复原有光标。
- 子进程故意跳过清理直接退出，确认独立恢复进程能恢复原有光标。
- 下面 1.1 的浮窗实现和对应测试代码已移除，其记录仅作为历史留存。

## 1.1 鼠标旁状态提示

- 新增读取 / 转换 / 保存时的旋转提示，成功勾选和失败提示。
- Release 构建 0 警告、0 错误。
- 新增反馈验证 10/10 通过，报告：`artifacts/feedback-test/report.json`。
- 实际创建提示窗口验证：调用方线程阻塞时动画继续；读取、成功、失败均不抢前台焦点；成功自动消失；新任务不受旧任务的消失计时影响。
- 负坐标显示器工作区及边缘位置计算验证通过。读取、保存、成功、失败渲染图已检查，无文字裁切。
- 此前 1.0 的实际图片粘贴功能已经用户两次确认可用。下面原始记录中的逐项人工场景没有全部单独复核。

## 已完成

- .NET 8 Release 构建：0 警告、0 错误。
- Windows x64 自包含、压缩单文件发布成功。
- 发布后的 EXE 自检：10/10 通过，退出码 0。报告：`artifacts/published-self-test.json`。
- PNG 半透明/全透明像素往返、DIB 上下方向、零 Alpha 兼容、DIBV5 显式透明与非标准通道掩码、24 位行填充验证通过。
- 损坏与超大 DIB 拒绝、PNG 损坏后回退 DIB 验证通过。
- 8 个并发同名写入不覆盖已有文件，临时文件无残留；不存在的目录不会被创建或重定向。
- 只读 Shell COM 探测正确取得本机两个已打开资源管理器窗口的视图句柄及各自真实路径。
- 资源管理器焦点在搜索框时，目标判定返回不接管。

## 尚未完成的实机验收

Windows UI 自动化尝试点击资源管理器时，工具两次返回：
`point (240, 180) is over ChatGPT.exe "Chrome Legacy Window", not target window explorer.exe "通用小工具"`。
重新激活及刷新后仍失败，因此停止界面操作，没有将这些场景标记为通过：

- 实际截图 → 桌面 Ctrl + V 的完整流程。
- 浏览器复制图片 → 普通文件夹 Ctrl + V 的完整流程。
- 地址栏、重命名、普通文件复制及其他软件粘贴不受影响。
- Windows 11 多标签页、长按去重、网络共享及权限错误。
- 托盘开机启动设置和系统重启后的恢复。

第一版对无法确认路径的场景会提示失败，不猜测目标目录。开机启动默认关闭。
